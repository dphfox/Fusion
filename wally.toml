[package]
name = "sour/lemon"
description = "A modern reactive UI library, built specifically for Roblox and Luau --dev"
license = "MIT"
authors = ["sour"]
version = "0.3.0-dev.1"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
